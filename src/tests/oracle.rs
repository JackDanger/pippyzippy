//! Oracle harness: compare bzippy2's output against the reference `7zz` CLI.
//!
//! The test helpers here are used by the 7zippy layer5 cross-validation tests,
//! not directly by bzippy2 itself. This module just provides the codec name
//! for use in test documentation.

// This module is intentionally minimal — bzippy2 level oracle tests
// run at the 7zippy integration layer (layer5_cross.rs), not here.
